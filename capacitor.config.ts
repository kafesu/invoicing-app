import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.halogenapps.simpleinvoices',
  appName: 'Simple Invoices',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
